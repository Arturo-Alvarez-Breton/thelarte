# Railway-specific profile settings - FORCE POSTGRESQL ONLY
logging.level.com.thelarte=INFO
logging.level.org.springframework.jdbc=DEBUG
logging.level.com.zaxxer.hikari=DEBUG

# FORCE PostgreSQL Configuration - Override any H2 settings
spring.datasource.url=jdbc:postgresql://shortline.proxy.rlwy.net:34465/railway
spring.datasource.username=postgres
spring.datasource.password=TejMCJvoFvrRlIDlIYseMVwDZhisXhLB
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration for PostgreSQL - FORCE
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Connection Pool Settings optimized for Railway
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Flyway Configuration - ENABLED for PostgreSQL
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migrations
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# SSL Configuration for Railway (required for public network)
spring.datasource.hikari.data-source-properties.sslmode=require

# EXPLICITLY DISABLE H2 - Override any H2 settings
spring.h2.console.enabled=false
spring.h2.console.path=

# Server Configuration for Railway
server.port=${PORT:8080}
server.address=0.0.0.0

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Secret
auth.jwt.secret=thisisasecretkeyforjwtauthenticationthelarteappthisneedstobe64chars

# Static resources
spring.web.resources.static-locations=classpath:/static/,file:uploads/
